// console.log(__dirname)
// console.log(__filename)


// ES5 (CommanJS) / ES6 (ECMA) import export

// core modules (in-built) , local modules (user defined) , third party modules (readyment)
// http, path, fs, os, url, buffer, crypto
// npm install

// const {addition, square, product, substraction, division} = require('./abc.js');

// console.log(addition(10,20))

// const {division} = require('./abc');
// console.log(division(55,11));

const os = require('os');

// console.log(os.networkInterfaces())
console.log(os.hostname())